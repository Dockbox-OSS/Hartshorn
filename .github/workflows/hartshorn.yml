name: Hartshorn

on: pull_request

# ┌───────────────┐     ┌──────────────┐     ┌───────────────┐
# │ License check │  →  │   Build all  │  →  │   Build all   │
# └───────────────┘     │ (JDK source) │     │  (JDK target) │
#                       └──────────────┘     └───────────────┘
#                               ↓                    ↓
#                       ┌──────────────┐     ┌───────────────┐
#                       │   Test all   │  →  │    Test all   │
#                       │ (JDK source) │     │  (JDK target) │
#                       └──────────────┘     └───────────────┘
#                               ↓
#                      ┌─────────────────┐
#                      │ Module coverage │
#                      │   (JDK source)  │
#                      └─────────────────┘
jobs:
  license:
    name: License check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up source JDK
        uses: actions/setup-java@v1
        with:
          java-version: 17
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: Check license headers
        run: ./gradlew checkLicenses
  buildSource:
    needs: [license]
    name: Build all (source JDK)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up source JDK
        uses: actions/setup-java@v1
        with:
          java-version: 17
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: "Build (source JDK)"
        run: ./gradlew build -x test
  buildTarget:
    needs: [buildSource]
    strategy:
      fail-fast: false
      matrix:
        java: [18, 19]
    name: Build all (target JDK ${{ matrix.java }})
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: "Build (Java ${{ matrix.java }})"
        run: ./gradlew build -x test
  testSource:
    needs: [buildSource]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    name: Unit tests (source JDK)
    runs-on: "${{ matrix.os }}"
    steps:
      - uses: actions/checkout@v2
      - name: Set up source JDK
        uses: actions/setup-java@v1
        with:
          java-version: 17
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: "Unit testing (OS: ${{ matrix.os }}, source JDK)"
        run: ./gradlew test
  testTarget:
    needs: [testSource, buildTarget]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        java: [18, 19]
    name: Unit tests (target JDK ${{ matrix.java }})
    runs-on: "${{ matrix.os }}"
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.java }}
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: "Unit testing (OS: ${{ matrix.os }}, Java: ${{ matrix.java }})"
        run: ./gradlew test
  coverage:
    needs: [testSource]
    strategy:
      fail-fast: false
    name: Code coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up source JDK
        uses: actions/setup-java@v1
        with:
          java-version: 17
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
      - name: Run tests
        run: ./gradlew test
      - name: Add coverage to PR
        id: jacoco
        uses: madrapps/jacoco-report@v1.3
        with:
          title: Code coverage
          update-comment: true
          paths: |
            ${{ github.workspace }}/hartshorn-cach/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-cache/hartshorn-cache-caffein/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-command/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-confi/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-config/hartshorn-config-jackso/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-cor/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-discover/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-event/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-hs/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-i18/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-introspec/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-introspect/hartshorn-introspect-reflectio/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-jp/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-jpa/hartshorn-jpa-hibernat/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-mv/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-mvc/hartshorn-mvc-freemarke/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-prox/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-proxy/hartshorn-proxy-cgli/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-proxy/hartshorn-proxy-javassis/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-reportin/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-test-suit/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-uti/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-we/build/reports/jacoco/test/jacocoTestReport.xml,
            ${{ github.workspace }}/hartshorn-web/hartshorn-web-jetty/build/reports/jacoco/test/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 80
          min-coverage-changed-files: 80
      - name: Verify coverage report
        run: ./gradlew jacocoTestCoverageVerification