apply from: "$rootDir/parent.gradle"

dependencies {
    include(project(':hartshorn-exceptions'))
    include(project(':hartshorn-bootstrap'))

    include "org.reflections:reflections:$reflectionsVersion"
}
