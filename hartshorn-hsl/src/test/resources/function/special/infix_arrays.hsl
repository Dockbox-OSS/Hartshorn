var length = 2;

fun arrayContains(arr, obj) {
   for (var o in arr) {
      if (o == obj) {
         return true;
      }
   }
   return false;
}

var arr = ["One", "Two"];
infix fun contains(arr, obj) {
    return arrayContains(arr, obj);
}

print(arr contains "One");
