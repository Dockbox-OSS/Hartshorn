var list = 1..101;
var fizzBuzz = [
    x % 15 == 0 ? "FizzBuzz" :
        (x % 3 == 0 ? "Fizz" :
            (x % 5 == 0 ? "Buzz" : x)
        ) for x in list];

for (var i in fizzBuzz) {
    print(i);
}