plugins {
    id 'org.spongepowered.plugin' version '0.9.0'
}

archivesBaseName = 'DarwinServer Modules'
group = project.pluginGroup
description = 'Functional modules to be wrapped using the DarwinServer plugin'

repositories {
    maven {
        name 'fastasyncworldedit'
        url 'https://ci.athion.net/job/FastAsyncWorldEdit/ws/mvn'
    }
    maven {
        name 'jitpack'
        url 'https://jitpack.io'
    }
}

dependencies {
    shadow project(':Core')
    compileOnly 'com.sk89q.worldedit:worldedit-core:6.1.4-SNAPSHOT'
    compile 'com.boydti:fawe-api:latest'
    compile 'com.zaxxer:HikariCP:3.1.0'
    compile group: 'io.netty', name: 'netty-buffer', version: '4.1.48.Final'
}

sponge.plugin.id = pluginId
